import { FileToGenerate } from "./utils";
/**
 * Reads all files inside inputPath
 *
 * @param inputPath
 * @param supportedLanguages
 * @param outputPath
 */
export declare const generate: (inputPath: string, defaultLanguage: string, supportedLanguages: string[], outputPath?: string) => {
    filesToGenerate: FileToGenerate[];
    timeToProcess: number;
};
