---
import type Article from "@/core/article/article.model";
import SharedArticle from "../../../../packages/shared/src/layouts/Article.astro";

interface Props extends Article {
	url: string;
	minutesRead: string;
	enableComments?: boolean;
	slug?: string;
}

const props = Astro.props as Props;
---

<SharedArticle {...props}>
	<slot />
</SharedArticle>
