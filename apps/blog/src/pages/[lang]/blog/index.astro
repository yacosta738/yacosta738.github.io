---
import { ROUTE_LOCALES } from "@/i18n";

export const getStaticPaths = () =>
	ROUTE_LOCALES.map((lang) => ({
		params: { lang },
	}));

return Astro.redirect(
	`/${Astro.params.lang}${new URL(Astro.request.url).search}`,
	301,
);
---
