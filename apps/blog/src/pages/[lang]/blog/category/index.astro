---
import { ROUTE_LOCALES } from "@/i18n";

export const getStaticPaths = () =>
	ROUTE_LOCALES.map((lang) => ({
		params: { lang },
	}));

const search = new URL(Astro.request.url).search;
return Astro.redirect(`/${Astro.params.lang}/category${search}`, 301);
---
