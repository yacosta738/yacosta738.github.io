---
import type { ComponentProps } from "astro/types";
import SharedFeaturedProjects from "../../../../../packages/shared/src/components/sections/FeaturedProjects.astro";

const props = Astro.props as ComponentProps<typeof SharedFeaturedProjects>;
---

<SharedFeaturedProjects {...props}>
	<slot />
</SharedFeaturedProjects>
