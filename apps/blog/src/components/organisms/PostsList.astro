---
import type { ComponentProps } from "astro/types";
import SharedPostsList from "../../../../../packages/shared/src/components/organisms/PostsList.astro";

const props = Astro.props as ComponentProps<typeof SharedPostsList>;
---

<SharedPostsList {...props}>
	<slot />
</SharedPostsList>
