---
import type { ComponentProps } from "astro/types";
import SharedTagBadge from "../../../../../packages/shared/src/components/atoms/TagBadge.astro";

const props = Astro.props as ComponentProps<typeof SharedTagBadge>;
---

<SharedTagBadge {...props}>
	<slot />
</SharedTagBadge>
