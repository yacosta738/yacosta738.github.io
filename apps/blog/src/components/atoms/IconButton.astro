---
import type { ComponentProps } from "astro/types";
import SharedIconButton from "../../../../../packages/shared/src/components/atoms/IconButton.astro";

const props = Astro.props as ComponentProps<typeof SharedIconButton>;
---

<SharedIconButton {...props}>
	<slot />
</SharedIconButton>
