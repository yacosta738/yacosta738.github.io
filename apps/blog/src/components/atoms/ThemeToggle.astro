---
import type { ComponentProps } from "astro/types";
import SharedThemeToggle from "../../../../../packages/shared/src/components/atoms/ThemeToggle.astro";

const props = Astro.props as ComponentProps<typeof SharedThemeToggle>;
---

<SharedThemeToggle {...props}>
	<slot />
</SharedThemeToggle>
